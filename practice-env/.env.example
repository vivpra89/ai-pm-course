# Optional: only needed when you call cloud LLM APIs from local scripts
# (text-to-SQL experiments, RAG, chatbot). Not required for SQL + plots labs.
#
# OPENAI_API_KEY=
# ANTHROPIC_API_KEY=

# Default DB path (override if you move the file)
# CLOUDNOTE_DB=./data/cloudnote.db
